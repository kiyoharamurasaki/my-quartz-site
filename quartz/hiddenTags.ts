/**
 * Tags listed here will be hidden from public display:
 * - TagList component (article tag badges)
 * - Graph view (tag nodes)
 *
 * These tags are still usable internally in Obsidian for organization.
 * To add a new hidden tag, simply append it to the array below.
 */
export const HIDDEN_TAGS: string[] = ["public"]
